#include <stdio.h>
#include <stdlib.h>

int main()
{
    int sum,num[ ]={10,11,12,13,14};

    printf("%d \n",num[0]);

    sum=num[0] +num[1] + num[2] + num[3] + num[4];
    printf("The sum is: %d ",sum);
    return 0;
}
